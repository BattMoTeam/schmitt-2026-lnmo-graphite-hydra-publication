# Python dependencies for interoperability workflows.
# Install with:
#   python -m pip install -r requirements.txt

pybamm[bpx]
# BPX 1.0 schema (the bpx 1.1 series uses a different document layout).
bpx==0.5.0
numpy
scipy
matplotlib
